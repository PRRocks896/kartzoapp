export interface feeStateRequest {
    count: string,
    currentPage: string,
    items_per_page: string,
    upperPageBound: number,
    lowerPageBound: number,
    pageBound: number,
    onItemSelect: string,
    feedata: any,
    switchSort: boolean,
    isStatus: boolean
}